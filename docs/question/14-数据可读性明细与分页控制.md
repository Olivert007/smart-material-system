# 数据可读性、明细展示与分页控制

- 日期：2026-08-10
- 状态：**W1–W5 已闭环**；**DT-W6 已闭环**（按表 `TABLE_FIELD_ZH`）
- 对象：`browse` / `ask` / `export` API；`BrowseView`、`PagedTable`、`AskView`

---

## 已落地

| 阶段 | 内容 | 落点 |
|---|---|---|
| **DT-W1** | `v_browse_*` 四视图；`GET /browse/{table}?mode=business`（默认） | `schema.py`、`reports.py` |
| **DT-W2** | 浏览每页 10/20/50/100，默认 20 | `BrowseView.vue`、`PagedTable.vue` |
| **DT-W3** | 问答 `total_rows` + `truncated` 提示 | `text2sql.py`、`AskView.vue` |
| **DT-W4** | 治理流水待确认 + 自学习资产四 Tab 统一 `PagedTable` | `GovernView.vue`、`AssetsView.vue` |
| **DT-W5** | 导出 `mode=business` | `reports.py` |

| **DT-W6** | `TABLE_FIELD_ZH` 补全 inventory/asset；browse/export 用 `zh_columns_for_table` | `field_dict.py`、`reports.py` |

---

## 遗留

| 阶段 | 任务 | 优先级 |
|---|---|---|
| DT-W7 | 同步 06 能力表 | P2 |
| — | 问答服务端翻页 API | P2 |

**登记**：[01](01-项目问题与进展.md) §七 #14、#18–#19、#22。
