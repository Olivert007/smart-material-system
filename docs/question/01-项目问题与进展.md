# 项目问题与进展（给开发 Agent 的反馈）

- 日期：2026-08-09（第 4 版：补充解决方案与交叉索引；第 3 版对照代码复核）
- 对象：`smart-material-system` 实现代码 vs `治理方案` 文档
- 用途：供开发 Agent 对照整改；文末「待处理清单」为明确行动项
- **解决方案总表**：[00-交叉分析与解决方案索引](00-交叉分析与解决方案索引.md) · **文档地图**：[README](README.md)

---

## 一、总体进展定位

**Phase A 可信链路已闭环；Phase B PoC 验收通过（带备注）；Phase D 治理产品化基本成型；实施计划（roadmap/implementation-plan.md P1–P4）已全部落地（2026-08-09 06:00–07:02）；分析面已启动建设；Stage 2 24h 双常驻 gate 仍未 claim。**

| 阶段 | 内容 | 进展 |
|---|---|---|
| A 基础可信链路 | meta SQLite、唯一 writer、幂等发布、AST 只读查询、备份、任务恢复 | ✅ 完成 |
| B 单模型智能 | 映射 / Text2SQL / 流水 LLM | ✅ PoC `PASS_WITH_NOTES`（20260809_phase_b.md） |
| C 多模型评估 | fast(7B)、互验、固定 FP8、24h gate | 🔶 SQL 样本已扩至 57（EVAL_SQL50_OK 通过）；FP8 已切换（big=Qwen3.6-27B-FP8 常驻）；24h gate 运行中（fast 受沙箱限 CPU 降级，GPU 双常驻未验证）未 claim |
| D 治理产品化 | 指标字典、血缘回滚、界面、FLOW_* 激活 | 🔶 大部分完成（**F4 LAN 权限 ✅ 2026-08-09**；FLOW_* 已于 2026-08-09 激活 active；14 项指标实测全 active） |
| P1–P4 实施计划 | blocked 明细、value_rule、rule_learn、发布生命周期、correction、报表快照、指标时序 | ✅ 代码全部落地；live `data/meta.sqlite` 已于 2026-08-09 用新代码启动迁移（备份 `meta.sqlite.bak_20260809T125914`，staging_blocked/value_rule/report_definition/report_run/metric_snapshot/correction_request 全部就位） |
| 分析面 | 首页业务快照、业务指标、Ask 图表、报表快照 | 🔶 已启动（详见 03 文档） |

**拓扑拍板（2026-08-09）**：国内网可直连 modelscope.cn 与两个 ModelScope 推理端点；API 默认 4 个端点（2 个 disabled）；worker 与 API 同进程。F1 失败时 `prompt_rebuild`，若模型不通则提示 `MODEL_UNAVAILABLE`。

**模块 12（流水 LLM）**：gate_ready=true、published 467 条 L1；live 库实测 `flow_pending` 无待解析行（555 行均为 confirmed/ignored 终态）、`flow_reconcile_gap=324`。物资对齐 gap 327→324 同前；其余歧义需人工确认。**FLOW_* 3 个质量门指标已于 2026-08-09 激活**（`scripts/activate_flow_metrics.py`，gate 6 项检查全过）：FLOW_QTY_TOTAL=1756、FLOW_PARSE_L1_RATIO=1.0、FLOW_RECONCILE_GAP_CNT=324，text2sql 模板应答已放行。

**业务数据（live 库实测 2026-08-09）**：`fact_stock_flow=467`（IN 合计 1756）、`fact_inventory=280`、`fact_asset=2305`、`fact_demand=76`、`dim_material=674`。

**评测（2026-08-09 最近一次）**：mapping 1.0（46/46，`填报人→keeper_or_user`/`单价→stock_value` 别名已修复，`stage1_20260809T141928Z`）、text2sql 1.0（57/57）、flow LLM 1.0（12/12）、真实问答 8/8；SQL 样本已扩至 57（`smoke_eval_sql50.py` 通过）。Stage2 24h gate 运行中（big GPU + fast CPU 降级，起点 13:10:18Z）。

**测试**：`tests/` 40 个用例，`pytest tests/` 全量通过（conftest 固化 env + 隔离 DATA_DIR 后无失败）。

---

## 二、已落实且验证通过的能力（按类别列举）

1. **唯一事实来源 / 原子写**：`meta.sqlite` 仅 writer 进程写；幂等发布 + flow_pending 落库。✅
2. **AST 只读查询**：SQL 解析白名单（`SELECT/JOIN/WHERE/GROUP_BY/ORDER_BY`）＋全路径校验，拦截非法读写。✅
3. **血缘回滚 / 重建**：release→file→reconcile 血缘，`RollbackType` 可见于 `govern/releases`。✅
4. **任务恢复**：worker heartbeat 复用（超时判定，配置化）。✅
5. **指标字典 + 激活**：`metric_dict` 实测 **14 项全部 active**（`BUSINESS_METRICS` 11 项：业务 9 项 INV_*×5/DEMAND/ASSET/FLOW_IN/FLOW_OUT + 运维 2 项 INTAKE_*×2；`FLOW_DRAFT_METRICS` 质量门 3 项已于 2026-08-09 激活）、`evaluate_metric`、血缘可达。✅
   - ⚠️ 用户视角评审（2026-08-09 第 3 版，Phase 0 已部分闭环）：`INTAKE_BLOCK_RATE/CLEAN_RATE` 属运维类指标混入业务组 → **已按 `metric_group` 分组（business 9/quality 3/ops 2）并首页只显 business**（U-1）；`INV_QTY_TOTAL` 跨单位求和口径无业务意义（U-2，待台账单位口径）；`INV_STALE_CNT`/`INV_VALUE_TOTAL` 依赖 `age_days`/`stock_value`（台账无此列）→ **已加 `data_check_sql` 探针，无有效数据时 `data_status=no_data` 前端显示「暂无数据」**（U-3 短期闭环）；`FLOW_QTY_TOTAL` **已更名「流水入库合计（质量门）」并收紧别名**（U-4）。详见 §六/§七。
6. **备份策略**：`scripts/backup.sh` 带 day/keep 天数。✅
7. **幂等 / 流水分类**：daily/intro/check 三类，std 入库 + explain。✅
8. **质量 / 差异视图**：`IntakeView` 差异分析、`GovernView` 质量清单、流水差异 gap 表。✅
9. **P1 清洗可观测（新增）**：`staging_blocked` 逐行拦截明细（与 staging 同 meta 事务写、重建时刷新）；`quality_report` / `quality_by_release` / `list_blocked` 端点（`/intake/quality/*`）；质量指标 `INTAKE_BLOCK_RATE`、`INTAKE_CLEAN_RATE` 已 active。前端 `StageView` 增加 Blocked 明细、按 file/release 质量摘要。✅
10. **P2 规则增强（新增）**：`value_rule` 表 + `apply_checks`（required / numeric_positive / id_exists / unique / …）接入 staging 落库；种子规则 3 条 active（`/govern/value-rules` CRUD）；`rule_learn` 只提案 `govern_confirm`（`/govern/rule-learn/propose|candidates|confirm`），确认后回写 rule_dict/value_rule。前端 `GovernView` 增加「规则学习」tab。✅
11. **P3 发布生命周期（新增）**：`release_manifest` 增加 `supersedes/superseded_by`（迁移兼容）；`/govern/release/diff`（基于 `fact_release_rows` 按 row_key 输出 added/removed/changed）；`/govern/release/supersede`；`correction_request` 单行修正通道（propose/apply/decline），修正 = 新 release + supersede，**禁原地 UPDATE**。前端 `OpsView` 增加发布生命周期（diff、supersede、修正）tab。✅
12. **P4 二次加工（新增）**：`report_definition/report_run` 报表快照（AST 只读校验 + parquet/csv 落盘 + `interval_sec` cron 进 worker；`/reports/*` 端点）；`metric_snapshot` 指标时序（evaluate 默认 write_snapshot，`/metrics/{id}/snapshots`）。前端新增 `ReportsView` 页。✅
13. **分析面（新增，详见 03 文档）**：`stats_overview.business_snapshot` 首页业务快照（库存总量/金额、定额利用率、超定额、呆滞料、需求总量、资产台数、入库/出库合计 + 按类别/库位 Top）；`BUSINESS_METRICS` 11 项 active（业务 9 + 运维 2，首页只显业务组）；Ask 页 ECharts 柱状/饼图自动渲染（`smoke_ask_chart.py` 可跑通）；`MetricsView` 支持快照趋势。**UI-*（2026-08-09 Phase 2）**：`/analytics/*` 三端点、worker 30min 业务快照、报表种子启动幂等就位、GovernView「流水分析」tab、HomeView 迷你趋势/定期报表卡、MetricsView 趋势弹窗（dist 待 node 重建）。✅

---

## 三、实质问题（P0，均已修复并验证）

| # | 问题 | 状态 |
|---|---|---|
| P0-1 | 服务内部临时写数据 | ✅ 修复：统一走唯一 writer + meta |
| P0-2 | 新任务未断点续跑 | ✅ 修复：pending 队列 + 恢复 |
| P0-3 | 幂等发布前检查、失败落库 | ✅ 修复 |
| P0-4 | 原始数据只读 / 唯一 writer | ✅ 修复 |
| P0-5 | 明细级错误 → 整批回滚（500+ 行） | ✅ 修复：staging 单行阻断，失败落 `staging_blocked` 可改可重试 |

---

## 四、中危偏差（应解决但未解决）

| # | 问题 | 状态 | 解决方案（摘要） |
|---|---|---|---|
| 中危 1 | 真实录入批不含金额列 → 汇总勾稽无法核对 | ✅（2026-08-10 方案 B） | **方案 B 已拍板**：文档明确「金额勾稽不做硬核验」，仅 FL6 数量勾稽；08 口径已更新；`INV_VALUE_TOTAL` 无有效数据时展示「暂无数据」（U-3） |
| 中危 2 | 物资编码标准不一致：同材异码、单位不一致 → 对齐 gap 327→324，需人工 | 🔶 已缓解，仍待人工 | 歧义清单导出 → GovernView 人工确认 → `rule_dict` 沉淀别名；长期建标准编码主数据。与 `flow_reconcile_gap=324` 联动治理 |
| 中危 3 | 评测规模小（SQL 仅 10 条）+ Stage2 未 claim | 🔶 SQL 57/57 ✅；gate 未 claim | 环境解限后 `models.sh start fast`（GPU）→ `stage2_gate.py start` **重开** 24h；**CPU fast 当前轮次不算 claim**（见 [02 §Stage2](02-模型问题反馈.md)） |
| 新增 4 | `pytest tests/` 全量 40 用例有 **6 失败** | ✅ 已解决 | 见 [05 测试工程问题](05-测试工程问题.md)；后续加 env 常量须同步 `conftest.py` |

---

## 五、低危（已复核，代码侧 06:00–07:02 未触及，全部仍开放）

| # | 问题 | 状态 |
|---|---|---|
| 低危 1 | `flow_parse.split_flow_text` 死代码（`pieces = year_cut.split(seg)` 结果未使用，后续改用 finditer） | ✅ 已修复：删除 `year_cut` 正则与死代码（2026-08-09） |
| 低危 2 | `text2sql.SCHEMA_ZH` 仍缺 `fact_stock_flow`、`fact_quota_adjust` 中文说明 | ✅ 已修复：补齐两表全列中文（2026-08-09） |
| 低危 3 | `fact_quota_adjust` 半成品（无 writer / release / 勾稽） | 🔶 已补全写入路径：`intake_plan._TARGET_TABLE` + `writer.cols_by_table` 支持 quota 域（血缘走通用镜像）；仍无业务样本端到端验证 |
| 低危 4 | `/models/status` 逐个端点探测 5s×3 串行 → 仪表盘首屏慢 | ✅ 已修复：`asyncio.gather + to_thread` 并发探测，最坏 5s（2026-08-09） |
| 低危 5 | 前端 `generated.ts` 未消费（client.ts 手写） | 🔶 决策保留：`api:check` 已保证 generated.ts 与 openapi.json drift 为零；client.ts 手写错误处理为设计决策，不强行重构 |
| 低危 6 | `config.OPS_TOKEN` 默认仍为 `dev-ops-token-change-me`（生产需 env 覆盖） | ✅ 已修复：`compose.prod.yml` 改用 `${OPS_TOKEN:?}` 强制设置（未设置拒绝启动），env.example 注明 |
| 低危 7 | `deploy/compose.prod.yml` 无独立 worker 服务（与 API 同进程） | ✅ 已决策关闭：worker 与 API 同进程为拓扑拍板（docs/09/11），compose 注释说明 |

---

## 六、用户视角评审新增项（2026-08-09 第 3 版）

### 6.1 指标与字段合理性（[metrics.py](file:///workspace/2026-07/smart-material-system/app/services/metrics.py) / [field_dict.py](file:///workspace/2026-07/smart-material-system/app/services/field_dict.py) 实测复核）

> 完整分步方案与涉及文件：[00 §3.2](00-交叉分析与解决方案索引.md#32-用户指标字段u)

| # | 发现 | 影响 | 建议 | 解决方案要点 |
|---|---|---|---|---|
| U-1 | 运维类指标（INTAKE_*×2、FLOW_PARSE_L1_RATIO、FLOW_RECONCILE_GAP_CNT）与业务指标混在同一字典，且出现在首页「业务指标」表 | 库管员看到"阻断率/clean率"等黑话 | 指标字典加 `group`（business/quality/ops），首页只渲染 business | **Phase 0**：`metric_dict.metric_group` 迁移 + 种子标注 + `HomeView`/`MetricsView` 默认过滤（✅ 2026-08-09 落地） |
| U-2 | `INV_QTY_TOTAL`=SUM(stock_qty) 跨单位（件/米/对/包）直接求和；`business_snapshot` 同源 | 数字无业务含义 | 改按单位分组或按价值口径 | **短期 ✅（2026-08-09）**：`business_snapshot.top_by_unit` 按单位 Top，HomeView「按单位 Top」表展示；长期按台账单位口径（G-1）后指标 SQL 再细化 |
| U-3 | `INV_STALE_CNT` 依赖 `age_days`、`INV_VALUE_TOTAL` 依赖 `stock_value`，台账无对应列 | 恒 0/恒空，用户视为无效指标 | 短期 unavailable；长期 T1 补列 | `evaluate_metric` 返回 `data_status`；前端显示「暂无数据」（✅ 短期闭环 2026-08-09：`data_check_sql` 探针 + `no_data`） |
| U-4 | `FLOW_QTY_TOTAL` 别名含「出入库总量」实为仅入库，与 `FLOW_IN_QTY_TOTAL` 重复 | 名实不符/问答误命中 | **不删 metric_id**（gate 依赖） | 改 `metric_name`、收紧 `aliases`、`group=quality`；业务问法只命中 `FLOW_IN_QTY_TOTAL` |
| U-5 | 缺用户核心指标：最低库存预警（min_qty）、应急备汛定额满足率、定额达成分布、周转率 | 分析面缺口 | ledger T1/T2 后新增 `INV_BELOW_MIN_CNT` 等 | 依赖 G-1 字段落地，见 ledger-export-plan T7 后 |
| U-6 | 字段汉化：location/custodian/check_result 按表语义不同；flow_type IN/OUT 未汉化 | 用户困惑 | `TABLE_FIELD_ZH` + 值域字典 | **✅** `VALUE_ZH`（IN/OUT）+ `TABLE_FIELD_ZH` fact_inventory/asset（DT-W6 2026-08-10） |

### 6.2 4 表台账汇总导出能力差距（新 roadmap：[roadmap/ledger-export-plan.md](file:///workspace/2026-07/smart-material-system/roadmap/ledger-export-plan.md)）

> 完整任务链与 LD-1~6 决策：[ledger-export-plan](../../roadmap/ledger-export-plan.md) · [00 §3.3](00-交叉分析与解决方案索引.md#33-台账能力g)

| 缺口 | 说明 | 任务 | 解决方案要点 |
|---|---|---|---|
| G-1 | 备品备件所属系统/项目名称/消耗计划/物资来源、公用工器具数量/是否仪器仪表/更换检测周期、应急备汛新集团编码/框架物资/供应商 无 schema 落点 | T1 | `fact_inventory`/`fact_asset` 增可空列；LD-1/LD-2 先锁定 |
| G-2 | [evidence.py](file:///workspace/2026-07/smart-material-system/app/services/evidence.py#L260-L311) 多 sheet 路由只吸收带出入库记录的表，**公用工器具/应急备汛两 sheet 被丢弃** | T3 | `ledger_route` 配置驱动逐 sheet 路由；不再「无流水列即丢弃」 |
| G-3 | 输出端无"固定模板列"台账导出（现有 `/export/table` 为标准表、报表为自定义 SQL） | T7/T8 | 4 条 `report_definition` 种子 + 可选 `/export/ledger/{sheet}` |
| G-4 | 维护材料"现有库存"含 `50+` 等非数字值、单位混用（件/米/对/包），勾稽需清洗规则 | T5 | `value_rule`：`50+`→50，原值进 `fact_release_rows`；违规则 `staging_blocked` |

---

## 七、待处理清单（按优先级）

> 执行顺序见 [00 §4](00-交叉分析与解决方案索引.md#4-执行阶段编码顺序)：Phase 0 快赢 → Phase 1 台账 → Phase 2 数据质量/UI → Phase 3 基础设施 → Phase 4 采购域。

1. [✅] **修复 pytest 全量隔离**：见 [05](05-测试工程问题.md)。
2. [🔵] **Stage2 24h gate**：SQL 57/57 ✅；当前轮次 big=FP8 GPU + fast=CPU 降级（**不算 claim**）。解限后：`models.sh start fast`（GPU）→ `stage2_gate.py start` 重开 → 期满 `claim`。详见 [02](02-模型问题反馈.md) §Stage2。
3. [✅] **FP8 权重下载并切换**：2026-08-09T13:10:18Z 已就位（`/models/Qwen3.6-27B-FP8`，`start_vllm_big27.sh` 默认）。保留 bf16 回退：`MODEL_DIR=/models/Qwen3.6-27B bash scripts/start_vllm_big27.sh`（M-1）。
4. [ ] **中危 1**：金额列硬核验——业务决策 A（补列）或 B（文档明确不做）。见 [00 §3.1](00-交叉分析与解决方案索引.md#31-工程技术债01)。
5. [ ] **中危 2**：物资编码标准域 + 人工确认流程。见 [00 §3.1](00-交叉分析与解决方案索引.md#31-工程技术债01)。
6. [ ] **低危 1–7**：✅ 1/2/4/6/7；🔶 3（待 quota 样本）、5（决策保留）。
7. [✅] **FLOW_* 指标激活**（2026-08-09）；U-4 展示整改与激活不冲突——改 group/别名，不删 id。
8. [✅] **live meta.sqlite 迁移**（2026-08-09，备份 `meta.sqlite.bak_20260809T125914`）。
9. [✅] **4 表台账汇总导出（G-\*）**：LD-1~6 已锁定；T1~T10 代码已落地（2026-08-10），`pytest` 80 passed；待生产环境周期性重放验收。详见 [ledger-export-plan](../../roadmap/ledger-export-plan.md)。
10. [✅] **指标与字段整改（U-*，Phase 0）**：U-1/U-3/U-4 ✅（2026-08-09）；U-6 🔶（机制+`VALUE_ZH` ✅，按表 `TABLE_FIELD_ZH` 待 T1/T2）；**U-2 短期 ✅**（2026-08-09：`business_snapshot.top_by_unit` 按单位 Top，HomeView 已展示；长期待台账单位口径 G-1）；U-5 待台账。见 [00 §3.2](00-交叉分析与解决方案索引.md#32-用户指标字段u)。
11. [✅] **分析面 UI（UI-*）**（2026-08-09）：**后端** `analytics.py` 新增 `/analytics/flow-monthly|flow-top|flow-level`（只读）；worker 每 30min `snapshot_business_metrics()`（business 组，`METRIC_SNAPSHOT_MINUTES` 可配）；`ensure_report_seed` 启动幂等落 4 条种子报表（含带参示例 `rpt_inv_filtered`）+ cron `0 8 * * *` 解析修复。**UI-4 参数化（2026-08-09）**：`run_report` 支持 `${param}` 占位替换（白名单校验 + 引号包裹，渲染后二次过 AST 校验，缺参/非法值报错），`POST /reports/{id}/run` 收 `{params}` body，ReportsView 按 `params_json` 渲染动态表单。**前端** HomeView 迷你趋势（UI-2）+「定期报表」卡（UI-5）+「按单位 Top」表（U-2）、GovernView「流水分析」tab（UI-1）、MetricsView「趋势」弹窗（UI-3）。pytest 40 passed，端点实测 200。**前端 dist 待 node 环境重建后生效**。见 [03 §四](03-界面分析面缺失（使用系统角度）.md)。
12. [✅] **F4 LAN 权限**（2026-08-09）：`ALLOWED_CIDRS` 中间件（`LanGuardMiddleware`，非空时回环 + CIDR 放行，其余 403）+ compose.prod.yml / start_api.sh / env.example 注入。
13. [✅] **模型运维（M-*）**（2026-08-09）：`models.sh` 按需起停 `status|start|stop [big|fast|embed|all]` + `mem-guard`（停 embed→fast，不停 big，日志 `data/eval/mem_guard_*.json`）+ `EMBED_DEVICE=cpu`（M-3）；`models_watchdog.py` 扩展 `--guard`；06 运维手册 §1.4 已更新。见 [02 §五–§七](02-模型问题反馈.md)。
14. [✅] **DT-W1 业务明细视图**（2026-08-10，[14](14-数据可读性明细与分页控制.md) §四）：`v_browse_*` 四视图（JOIN dim_material 名称/规格/单位前置）+ `/browse` 默认 `mode=business`（raw 可回物理表）；顺带修复 browse/export `flow_type` 值域汉化未生效 bug。新增 `test_browse_business.py` 5 用例，`pytest tests/` 73 passed。
15. [✅] **OV-* 总览瘦身**（2026-08-10，[11](11-总览页用户Review与整改方案.md)）：删除底部导航/模型卡/报表种子表；业务快照与表行数分区；最近文件 1 条摘要；链入 `/data`。`HomeView.vue` 重写。
16. [✅] **RP-* 报表用户向收口**（2026-08-10，[12](12-报表页用户向与报表定义暴露问题.md)）：`ReportsCatalog.vue` 无 SQL/report_id；运行结果摘要+下载；挂入 `DataView` `tab=report`；`/reports` redirect。
17. [✅] **IA-W1–W5 六入口收敛**（2026-08-10，[13](13-页面功能收敛与信息架构.md)）：侧栏 总览/问数/数据中心/接入/治理/系统；`DataView`/`GovernHub`/`SystemView`；旧路由 redirect；`ops_role` 控制「系统」可见性。
18. [✅] **DT-W2/W3/W5 分页与截断**（2026-08-10，[14](14-数据可读性明细与分页控制.md)）：`PagedTable` 默认 20 条；问答 `truncated`+`total_rows`；导出 `mode=business`。
19. [✅] **DT-W4 全站分页统一**（2026-08-10）：`GovernView` 流水待确认 + `AssetsView` 四 Tab 接入 `PagedTable`（10/20/50/100）。
20. [✅] **RP-5 种子报表免 Token**（2026-08-10）：`require_ops_or_seed_report`；`ReportsCatalog` 移除前端 Token 拦截；`test_reports_seed_auth.py`。
21. [✅] **IA-W6 07 文档对齐**（2026-08-10）：`07-界面层设计.md` Phase 2.4，6 入口 + redirect + 功能矩阵。
22. [✅] **台账 T1 schema 验收**（2026-08-10）：`test_ledger_t1_schema.py` 断言 `fact_inventory`/`fact_asset` 扩列就位（实现见 ledger-export-plan T1）。
23. [✅] **台账 T2 映射 + DT-W6 按表汉化**（2026-08-10）：`frame_material_*` 别名；`TABLE_FIELD_ZH`；`zh_columns_for_table`；`test_ledger_t2_mapping.py` + 扩展 `test_ledger_samples`。

| 优先级 | 项 | 出处 |
|---|---|---|
| ~~P0~~ | ~~flow_config 接入解析管线或明确标注存档~~ ✅ | P0-3 |
| ~~P0~~ | ~~实现真实心跳超时恢复~~ ✅ | P0-4 |
| ~~P0~~ | ~~勾稽补齐期初口径或标注简化口径~~ ✅ | P0-5 |
| P1 | 评测样本扩至 50+（SQL），再 claim Stage2 24h gate | 中危 3（57/57 ✅ 24h 验证中） |
| ~~P1~~ | ~~上传批次/目录配额、LLM 重试、备份含 evidence/staging~~ ✅ | 中危 1/2/8 |
| ~~P1~~ | ~~confirm 带 staging_id；重建用 release 快照~~ ✅ | 中危 4/6 |
| ~~P1~~ | ~~A5.2 fact_release_rows.row_key 镜像~~ ✅ | 中危 5 |
| P2 | 低危打磨项逐条清理 | 五 |
| P2 | U-* / UI-* 用户视角整改（Phase 0 快赢） | [00 §4 Phase 0](00-交叉分析与解决方案索引.md#phase-0--快赢13-天无-schema-依赖) |
| P2 | G-* 台账能力（ledger-export-plan） | [00 §4 Phase 1](00-交叉分析与解决方案索引.md#phase-1--台账能力解锁-g-u-5) |

---

## 八、交叉分析摘要（第 4 版新增）

1. **工程完成度 ≠ 用户可用度**：P1–P4 已落地；U-1/U-3/U-4 已 Phase 0 闭环（2026-08-09），U-6 机制已落地（按表补全待 ledger T1/T2），U-2/U-5、G-1~G-4 仍开放；`business_snapshot` 与 `INV_QTY_TOTAL` 共用跨单位求和（U-2）。
2. **324 联动**：`flow_reconcile_gap=324` 与物资对齐 gap 同源数据质量；中危 2 应与治理中心差异清单联动，不宜只展示为指标数字。
3. **趋势能力分层**：`seed_reports` 已有按月趋势报表，但 HomeView/GovernView 未暴露（UI-5）；非「后端未做」。
4. **Stage2 口径**：CPU fast 轮次不得 claim；与 02、00 保持一致。
5. **采购与台账**：04 `fact_purchase_order` 与 ledger T1 共用 schema 扩展路径，立项前合并评审。

详情：[00-交叉分析与解决方案索引](00-交叉分析与解决方案索引.md)
