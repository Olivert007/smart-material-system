# 报表页用户向与「报表定义」暴露问题

- 日期：2026-08-10
- 状态：**✅ 已闭环**（RP-1～RP-5）
- 对象：`ReportsCatalog.vue`（`DataView` `tab=report`）

---

## 已落地

| 编号 | 内容 | 落点 |
|---|---|---|
| RP-1 | 报表目录无 SQL / `report_id` | `ReportsCatalog.vue` |
| RP-2 | 运行摘要 + 下载 | `ReportsCatalog.vue` |
| RP-3 | 移除「新建并运行示例」 | 未迁入 Catalog |
| RP-4 | 数据中心三 Tab + redirect | `DataView.vue`、`router` |
| RP-5 | 种子报表 `POST /reports/{id}/run` 免 Ops Token | `auth.py`、`reports.py`、`test_reports_seed_auth.py` |

---

## 遗留（P2）

- 运行结果表格预览前 N 行
- 目录行展示最近运行时间

**登记**：[01](01-项目问题与进展.md) §七 #16、#20。
