# 模块 12 · 实现任务清单（编码前拆解）

> 对照：[12-出入库流水解析.md](12-出入库流水解析.md) §9–§10  
> 工程根：[`../smart-material-system/`](../smart-material-system/)  
> **前提**：Phase A 可信链路可用（writer / staging / meta）；本清单 **先不写 LLM 路径**。  
> Doc 阶段（交叉引用）已完成；本清单从 **A-规则** 起排期。  
> 文件序列编号为 **13**（属模块 12 的实现拆解附录，避免与 `12-出入库流水解析.md` 同号）。  
> **进度（2026-08-08 收口，实现延续至 08-17）**：模块 12 **A/B/D1–D4 已闭环**（含 `FLOW_*` 固定夹具门禁 + `FLOW_D4_OK` 激活）；运维可用 `scripts/activate_flow_metrics.py`；验收映射见文末 §10。

状态约定：`待办` / `进行中` / `完成` / `阻塞`

---

## 0. 依赖与阻塞

| ID | 项 | 状态 | 说明 |
|---|---|---|---|
| B0 | Phase A 幂等发布可跑 | 完成 | `app/services/writer.py` + staging confirm |
| B1 | 客户确认借用口径（FL4） | 待确认 | PoC 默认 `OUT`+`[BORROW]`；若要独立枚举则改 schema/勾稽后再动指标 |
| B2 | 305B / ZW 样例文件可访问 | 完成 | `scripts/harden_flow_real.py` → `FLOW_HARDEN_OK`；四 sheet 覆盖 |

---

## A-规则（无 LLM）— 对应验收 1–4、6

### A1. Schema 与 meta 表对齐 12 §2 / §5

| 任务 | 落点 | 验收 |
|---|---|---|
| A1.1 扩展 DuckDB `fact_stock_flow` | `app/repositories/schema.py` | 增补：`remark`、`parse_level`、`parse_source`、`source_sheet`、`source_segment`；`flow_date` 用 DATE（或迁移策略）；保留 `release_id`/`source_release_id` 与现有 writer 一致 |
| A1.2 meta：`flow_pending` / `flow_example` /（可选）`flow_config` | `app/repositories/db.py` 或 schema 迁移 | 表结构可建；冲突不静默覆盖 |
| A1.3 旧表迁移策略 | writer / ensure_biz_schema | 缺列 `ALTER`；坏数据不原地改数量，走 A6 重建 |

**退出**：空库 `ensure_*` 后表齐；旧库可升级不炸。

### A2. 清洗原语（纯函数 + 单测）

| 任务 | 落点 | 验收 |
|---|---|---|
| A2.1 `split_flow_text` | 新建如 `app/services/flow_parse.py` | 多分隔符；「；+年份」切分样例通过 |
| A2.2 `extract_flow_fields` | 同上 | ≥5 种日期；数量模式；`已使用`→L3；年份不当 qty（§4.5） |
| A2.3 数量交叉校验 P1–P5 | 同上 | 单测覆盖：文本有数 / 纯日期借单列 / 多段无数→L2 不均分 |
| A2.4 夹具 | `tests/fixtures/flow_texts.json` + `tests/test_flow_parse.py` | 来自 12 §1.1 样例；禁止年份当 quantity 的负例 |

**退出**：单测绿；与 12 §4 规则一致。

### A3. `flow_config` 与映射接入

| 任务 | 落点 | 验收 |
|---|---|---|
| A3.1 配置模型 | `app/models/` Pydantic | 对齐 12 §3.1 JSON |
| A3.2 标准字段 | `app/services/mapping.py` / mapping_suggest | 识别 `flow_in_text` / `flow_out_text` / `clean=flow_text` |
| A3.3 305B/ZW 默认 config | `data/` 或 meta 种子 | 至少覆盖审计表中的出入库列 |

**退出**：给定 tabular + config 能指出文本列与 qty 列。

### A4. Step5 规则路径接入 staging

| 任务 | 落点 | 验收 |
|---|---|---|
| A4.1 在构建 domain 行时调用原语 | `app/services/mapping.py` 或 `staging.py` | `target_domain=stock_flow` 时一行文本→0..N 流水候选 |
| A4.2 L1→clean 候选；L2/L3→`flow_pending` | staging + meta 写 | L3 默认不进业务行；L2 策略按 12（入库+pending） |
| A4.3 `parse_source=rule` / 命中 example 则 `example` | flow_parse + meta 读 | 同 `text_norm` 复用 example，不调 LLM |
| A4.4 dry-run 报告含流水统计 | staging dry_run_json | L1/L2/L3 计数、待确认条数可见 |

**退出**：staging confirm 前可预览流水拆解结果。

### A5. Writer 发布 `stock_flow`

| 任务 | 落点 | 验收 |
|---|---|---|
| A5.1 幂等写入 `fact_stock_flow` | `app/services/writer.py` | `flow_id` 稳定；`release_id` 审计；重复 confirm 不双写 |
| A5.2 血缘键 | `fact_release_rows.row_key` + `fact_stock_flow` 列 | 发布/重建镜像 `source_file\|sheet\|row\|segment\|flow_type`；可对账 |

**退出**：端到端：证据→staging→confirm→DuckDB 可查。

### A6. 坏数据血缘重建（FL7，验收 6）

| 任务 | 落点 | 验收 |
|---|---|---|
| A6.1 识别错误流水（qty≈年份等） | `flow_lineage.audit` + `scripts/audit_stock_flow.py` + `GET /govern/flow/audit` | 报告可疑行 |
| A6.2 按 release 撤销/重建 | `flow_lineage.rebuild` + `scripts/rebuild_stock_flow.py` + `POST /govern/flow/rebuild` | **禁止** UPDATE quantity；`smoke_flow_a6` 通过 |

**退出**：历史错误集清理策略可执行（`FLOW_A6_OK`）。

### A7. 勾稽校验（验收 4）

| 任务 | 落点 | 验收 |
|---|---|---|
| A7.1 勾稽 SQL | `app/services/flow_gov.reconcile` | ΣIN−ΣOUT ≟ stock−COALESCE(opening,0)；阈值 0.01 |
| A7.2 差异清单持久化/导出 | meta 表或查询 API | CSV/JSON 可导出；差异允许非零 |
| A7.3 发布后触发（异步任务） | worker 钩子 | 不阻塞 release 成功路径（FL6） |

**退出**：对样例库跑出差异清单。

### A8. 治理 API（规则阶段最小集，验收 5 的 API 半截）

| 任务 | 落点 | 验收 |
|---|---|---|
| A8.1 `GET /api/v1/govern/flow/pending` | `app/api/routes.py` | 列表 L2/L3/冲突 |
| A8.2 `POST /api/v1/govern/flow/confirm` | 同上 + auth | 接受/修正/忽略；回写 `flow_example`；鉴权按 05 |
| A8.3 `GET/POST /api/v1/govern/flow/reconcile` | 同上 | GET 只读清单；POST(ops) 落库 `flow_reconcile_gap` |
| A8.4 OpenAPI → 前端类型 | 生成流程（11） | `api:check` 不漂 |

**退出**：用 curl/httpie 完成一次确认闭环；二次同文本走 example。

### A9. 质量统计与指标 draft（验收 1、7 前置）

| 任务 | 落点 | 验收 |
|---|---|---|
| A9.1 L1/L2/L3 占比查询 | `flow_gov.parse_stats`（含 by_source_file/sheet） | 可按 source_file/sheet 聚合 |
| A9.2 `FLOW_QTY_TOTAL` 保持 `draft` | `metric_dict` + `metrics.ensure_flow_metrics_draft`；激活走 gate | 门禁未过不得 `active`（08/12 §8）；`smoke_flow_a9` 403 |
| A9.3 可选 draft：`FLOW_PARSE_L1_RATIO` / `FLOW_RECONCILE_GAP_CNT` | 同上 + evaluate API | 只读可算；`FLOW_A9_OK` |

**退出**：基线数字可写进验收记录（`GET /govern/flow/baseline` / `scripts/flow_metrics_baseline.py`）。

---

## B-LLM（Phase B+）— 勿与 A 并行抢主路径

| 任务 | 落点 | 验收 |
|---|---|---|
| B1 pending 上结构化 JSON 建议 | `flow_llm.py` + worker + `POST /govern/flow/suggest` | 仅写 pending；`FLOW_B1_OK`；`FLOW_LLM_ENABLED=0` 可关 |
| B2 PolicyRouter：fast → escalate big | `policy_router.py`（task=`flow_parse_suggest`） | fast 不可用 degraded_up→big；低置信/坏 JSON escalate |
| B3 人工确认后 `parse_source=llm`→`manual` | `flow_gov.confirm_pending` | 已落：accept/amend 写 `manual` |
| B4 评测小集 | `flow_parse_llm.jsonl` + `scripts/run_eval_flow.py` | 阈值 `FLOW_EVAL_MIN_ACC=0.75`；`FLOW_EVAL_OK`（12/12） |

**退出**：LLM 关闭时 A 路径仍完整可用（`DISABLED_OK`）；评测小集可复跑。

---

## D-产品化（挂 07 / Phase D）

| 任务 | 落点 | 验收 |
|---|---|---|
| D1 治理中心 Tab「流水解析」 | `frontend/.../GovernView.vue` | 批量接受/修正/忽略；LLM 建议选中；example 只读 |
| D2 Tab「勾稽差异」 | 同上 | 重算 + CSV 导出 |
| D3 自学习资产 `flow_example` | Govern 流水 Tab 内只读表（Assets 页可后续独立） | 只读列表 |
| D4 `FLOW_QTY_TOTAL` → `active` | `metric_fixtures` + `POST /metrics/flow/activate` + `scripts/activate_flow_metrics.py` | 门禁全勾；`FLOW_D4_OK`；夹具隔离沙箱 |

---

## 建议排期（串行）

```
B0/B1/B2 确认
  → A1 schema
  → A2 原语单测          ← 可最先开干，不依赖 writer
  → A3 config
  → A4 staging 接入
  → A5 writer
  → A6 重建坏数据
  → A7 勾稽
  → A8 API
  → A9 统计/指标 draft
  →（Phase B）B1–B4
  →（Phase D）D1–D4
```

**第一刀编码建议**：只做 **A2（`flow_parse.py` + 单测）**，零基础设施依赖，立刻验证「年份不当 quantity」与多笔拆分。

---

## 与 12 §10 验收映射

| §10 | 主要由 |
|---|---|
| 1 L1/L2/L3 可统计 | A4 + A9 |
| 2 source 对账 | A1 + A5 |
| 3 无年份当 qty | A2 + A6 |
| 4 勾稽可导出 | A7 + A8.3 |
| 5 确认与 example | A8 + A4.3 |
| 6 血缘重建 | A6 |
| 7 指标 active | A9 + D4（门禁后） |

---

*完成一项在表内改状态即可；开编码时从 A2 起 PR，勿一次吞下 B/D。*
